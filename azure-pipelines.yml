# Bash

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure ARM'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'az group create --name rg-azuredevops-westeu-test-01 --location westeurope'

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure ARM'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'mkdir hello-inline'

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure ARM'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      az vm create \
        --resource-group rg-imagebuilder-weu-2 \
        --name vm-azure-devops-test-01 \