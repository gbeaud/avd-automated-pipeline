# Bash

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure ARM'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'az network nsg create --name nsg-azuredevops-westeu-test-05 --resource-group rg-imagebuilder-weu-2 --location westeurope'

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure ARM'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      pwd
      ls

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure ARM'
    scriptType: 'bash'
    scriptLocation: 'scriptPath'
    scriptPath: 'avd-azure-image-builder/test-azure-devops.azcli'